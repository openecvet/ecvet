CREATE TABLE qualification (ID INTEGER IDENTITY NOT NULL, DESCRIPTION LONGVARCHAR, TITLE VARCHAR, URI VARCHAR, UNITLIST_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE learningOutcome (ID INTEGER IDENTITY NOT NULL, DESCRIPTION LONGVARCHAR, REST LONGVARBINARY, TITLE VARCHAR, URI VARCHAR, PRIMARY KEY (ID))
CREATE TABLE referenceLevelList (ID INTEGER IDENTITY NOT NULL, DESCRIPTION LONGVARCHAR, TITLE VARCHAR, URI VARCHAR, PRIMARY KEY (ID))
CREATE TABLE unitList (ID INTEGER IDENTITY NOT NULL, DESCRIPTION LONGVARCHAR, TITLE VARCHAR, URI VARCHAR, PRIMARY KEY (ID))
CREATE TABLE unit (ID INTEGER IDENTITY NOT NULL, ACHIEVEMENT LONGVARBINARY, CREDIT INTEGER, DESCRIPTION LONGVARCHAR, RELATIVEWEIGHT INTEGER, TITLE VARCHAR, URI VARCHAR, WEIGHTSUM INTEGER, LEARNINGOUTCOMES_ID INTEGER, REFERENCELEVELLIST_ID INTEGER, UNITLIST_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE ecvetFramework (ID INTEGER IDENTITY NOT NULL, COMPETENTINSTITUTIONLIST LONGVARBINARY, COMPLEMENTARYDOCUMENTLIST LONGVARBINARY, DESCRIPTION LONGVARCHAR, TITLE VARCHAR, URI VARCHAR, QUALIFICATION_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE referenceLevel (ID INTEGER IDENTITY NOT NULL, DESCRIPTION LONGVARCHAR, FRAMEWORK VARCHAR, FRAMEWORKLEVEL INTEGER, FRAMEWORKTERM VARCHAR, INDICATION VARCHAR, TITLE VARCHAR, URI VARCHAR, PRIMARY KEY (ID))
CREATE TABLE learningOutcomeList (ID INTEGER IDENTITY NOT NULL, DESCRIPTION LONGVARCHAR, TITLE VARCHAR, URI VARCHAR, PRIMARY KEY (ID))
CREATE TABLE referenceLevelList_referenceLevel (referenceLevelList_id INTEGER NOT NULL, referenceLevel_id INTEGER NOT NULL, PRIMARY KEY (referenceLevelList_id, referenceLevel_id))
CREATE TABLE unitList_unit (unitList_id INTEGER NOT NULL, unit_id INTEGER NOT NULL, PRIMARY KEY (unitList_id, unit_id))
CREATE TABLE learningOutcomeList_learningOutcome (learningOutcome_id INTEGER NOT NULL, learningOutcomeList_id INTEGER NOT NULL, PRIMARY KEY (learningOutcome_id, learningOutcomeList_id))
ALTER TABLE qualification ADD CONSTRAINT FK_qualification_UNITLIST_ID FOREIGN KEY (UNITLIST_ID) REFERENCES unitList (ID)
ALTER TABLE unit ADD CONSTRAINT FK_unit_LEARNINGOUTCOMES_ID FOREIGN KEY (LEARNINGOUTCOMES_ID) REFERENCES learningOutcomeList (ID)
ALTER TABLE unit ADD CONSTRAINT FK_unit_REFERENCELEVELLIST_ID FOREIGN KEY (REFERENCELEVELLIST_ID) REFERENCES referenceLevelList (ID)
ALTER TABLE unit ADD CONSTRAINT FK_unit_UNITLIST_ID FOREIGN KEY (UNITLIST_ID) REFERENCES unitList (ID)
ALTER TABLE ecvetFramework ADD CONSTRAINT FK_ecvetFramework_QUALIFICATION_ID FOREIGN KEY (QUALIFICATION_ID) REFERENCES qualification (ID)
ALTER TABLE referenceLevelList_referenceLevel ADD CONSTRAINT rferenceLevelListreferenceLevelrferenceLevelListid FOREIGN KEY (referenceLevelList_id) REFERENCES referenceLevelList (ID)
ALTER TABLE referenceLevelList_referenceLevel ADD CONSTRAINT referenceLevelList_referenceLevelreferenceLevel_id FOREIGN KEY (referenceLevel_id) REFERENCES referenceLevel (ID)
ALTER TABLE unitList_unit ADD CONSTRAINT FK_unitList_unit_unit_id FOREIGN KEY (unit_id) REFERENCES unit (ID)
ALTER TABLE unitList_unit ADD CONSTRAINT FK_unitList_unit_unitList_id FOREIGN KEY (unitList_id) REFERENCES unitList (ID)
ALTER TABLE learningOutcomeList_learningOutcome ADD CONSTRAINT lrnngOutcomeListlearningOutcomelrningOutcomeListid FOREIGN KEY (learningOutcomeList_id) REFERENCES learningOutcome (ID)
ALTER TABLE learningOutcomeList_learningOutcome ADD CONSTRAINT larningOutcomeListlearningOutcomelearningOutcomeid FOREIGN KEY (learningOutcome_id) REFERENCES learningOutcomeList (ID)
